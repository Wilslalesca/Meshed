{
  "name": "coachmesh",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.7.0",
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "turbo": "2.1.2",
    "typescript": "5.5.4",
    "prettier": "3.3.3"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
