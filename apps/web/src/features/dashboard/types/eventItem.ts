export interface EventItem {
    id: string;
    title: string;
    date: string;
    time: string;
}

export interface CalendarEventItem {
    id: string,
    title: string,
    day: number,
    startTime: string,
    endTime: string
}