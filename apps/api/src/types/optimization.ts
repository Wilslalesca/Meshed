export type TimeOption = {
    start: string;
    end: string;
};

export type OptimizationDay = {
    day: string;
    options: TimeOption[];
};

export type TimeRange = {
    start: string;
    end: string;
};